# H Offset

H parameter for tool length offset.

**Summary**
Specifies which tool length offset to use. Referenced with G43 (tool length compensation on).

**Format**
`H<number>`

**Example**
```
G43 H1 Z10 (apply tool length offset #1)
```
