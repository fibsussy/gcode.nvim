# M19

Orient spindle - Stops spindle at specific angle.

**Summary**
Used before tool changes on some machines.

**Format**
`M19 S<degrees>` or `M19 R<degrees> P<direction>`

**Example**
```
M19 S0 (orient to 0 degrees)
```