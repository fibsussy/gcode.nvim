# G84

Right-hand tapping - Synchronized feed in, spindle reverse out.

**Summary**
Spindle and Z feed synchronized. Requires rigid tapping capable machine.

**Format**
`G84 <axes> R<retract> Z<depth> F<pitch> S<RPM>`