# G43

Tool length offset - Applies tool table offsets.

**Summary**
Does not cause motion. Next compensated axis move uses offset.

**Format**
`G43` or `G43 H<tool#>`

**Example**
```
T1 M6 G43 (load tool 1 with offsets)
G43 H2 (apply tool 2 offsets instead)
```